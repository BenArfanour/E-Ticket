{% extends '::admin.html.twig' %}

{% block body %}
    <h1>Modification d'une Billet</h1>


    {{ form(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('produit_index') }}">
                Retour
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
